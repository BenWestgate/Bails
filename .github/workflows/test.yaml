name: Functional tests

on:
  pull_request:
  push:
    branches: [ master ]

jobs:
  functional-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run shell test harness
        run: ./test/run.sh
